export interface SchoolSummary {
  id: string;
  name: string;
  address: string;
  contactEmail: string;
}

export interface SchoolDetail {
  id: string;
  name: string;
  address: string;
  contactEmail: string;
  isActive: boolean;
}
